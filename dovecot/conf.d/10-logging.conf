# =============================================================================
# 10-logging.conf — send logs to stderr so `docker logs dovecot` shows them.
# =============================================================================
log_path = /dev/stderr
info_log_path = /dev/stderr
debug_log_path = /dev/stderr

# Helpful timing info while benchmarking: log slow auth and mail operations.
auth_verbose = no
mail_debug = no
